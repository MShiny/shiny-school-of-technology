<%= form_with model: course, class: "form-card" do |form| %>
  <%= render "shared/form_errors", object: course %>

  <div class="form-section">
    <p class="form-section-title">Basics</p>
    <div class="mb-3">
      <%= form.label :subject_id, "Subject", class: "form-label" %>
      <%= form.collection_select :subject_id, Subject.ordered, :id, :name, { prompt: "Select a subject" }, class: "form-select" %>
    </div>

    <div class="mb-3">
      <%= form.label :title, class: "form-label" %>
      <%= form.text_field :title, class: "form-control", placeholder: "e.g. Programming for Everybody" %>
    </div>

    <div class="row">
      <div class="col-md-6 mb-3 mb-md-0">
        <%= form.label :provider, class: "form-label" %>
        <%= form.text_field :provider, class: "form-control", placeholder: "e.g. Coursera" %>
      </div>
      <div class="col-md-6">
        <%= form.label :url, class: "form-label" %>
        <%= form.url_field :url, class: "form-control", placeholder: "https://..." %>
      </div>
    </div>
  </div>

  <div class="form-section">
    <p class="form-section-title">Status &amp; Progress</p>
    <div class="row">
      <div class="col-md-4 mb-3 mb-md-0">
        <%= form.label :status, class: "form-label" %>
        <%= form.select :status, Course.statuses.keys.map { |s| [ s.humanize, s ] }, {}, class: "form-select" %>
      </div>
      <div class="col-md-4 mb-3 mb-md-0">
        <%= form.label :level, class: "form-label" %>
        <%= form.text_field :level, class: "form-control", placeholder: "e.g. E1" %>
      </div>
      <div class="col-md-4">
        <%= form.label :progress_percentage, "Progress %", class: "form-label" %>
        <%= form.number_field :progress_percentage, class: "form-control", min: 0, max: 100 %>
      </div>
    </div>

    <div class="mt-3">
      <%= form.label :roadmap_position, "Roadmap position", class: "form-label" %>
      <%= form.number_field :roadmap_position, class: "form-control", min: 1, style: "max-width: 10rem;" %>
      <div class="form-text">Optional. Used to manually order this course within its subject's roadmap.</div>
    </div>
  </div>

  <div class="form-section">
    <p class="form-section-title">Notes</p>
    <div class="mb-3">
      <%= form.label :purpose, "Purpose", class: "form-label" %>
      <%= form.text_area :purpose, class: "form-control", rows: 2, placeholder: "What are you doing this course for? e.g. Complete Python E1 foundation" %>
    </div>

    <div>
      <%= form.label :notes, class: "form-label" %>
      <%= form.text_area :notes, class: "form-control", rows: 3 %>
    </div>
  </div>

  <div class="d-flex gap-2 mt-4">
    <%= form.submit class: "btn btn-primary" %>
    <%= link_to "Cancel", (course.persisted? ? course : courses_path), class: "btn btn-outline-secondary" %>
  </div>
<% end %>
