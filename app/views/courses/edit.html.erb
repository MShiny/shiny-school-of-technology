<% content_for :title, "Edit #{@course.title}" %>

<div class="page-header">
  <div>
    <p class="page-eyebrow">Courses</p>
    <h1 class="h2">Edit Course</h1>
  </div>
</div>

<%= render "form", course: @course %>
