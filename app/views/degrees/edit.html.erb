<% content_for :title, "Edit #{@degree.name}" %>

<div class="page-header">
  <div>
    <p class="page-eyebrow">Degrees</p>
    <h1 class="h2">Edit Degree</h1>
  </div>
</div>

<%= render "form", degree: @degree %>
