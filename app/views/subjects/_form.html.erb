<%= form_with model: subject, class: "form-card" do |form| %>
  <%= render "shared/form_errors", object: subject %>

  <div class="form-section">
    <p class="form-section-title">Basics</p>
    <div class="mb-3">
      <%= form.label :name, class: "form-label" %>
      <%= form.text_field :name, class: "form-control", placeholder: "e.g. Machine Learning" %>
    </div>

    <div class="mb-3">
      <%= form.label :description, class: "form-label" %>
      <%= form.text_area :description, class: "form-control", rows: 3 %>
    </div>

    <div class="mb-3">
      <%= form.label :status, class: "form-label" %>
      <%= form.select :status, Subject.statuses.keys.map { |s| [ s.humanize, s ] }, {}, class: "form-select", style: "max-width: 16rem;" %>
    </div>

    <div>
      <%= form.label :notes, class: "form-label" %>
      <%= form.text_area :notes, class: "form-control", rows: 2 %>
    </div>
  </div>

  <div class="form-section">
    <p class="form-section-title">Degrees</p>
    <div class="row row-cols-2 row-cols-md-3 g-2 border rounded-3 p-3">
      <%= form.collection_check_boxes :degree_ids, Degree.ordered, :id, :name do |b| %>
        <div class="col form-check">
          <%= b.check_box(class: "form-check-input") %>
          <%= b.label(class: "form-check-label") %>
        </div>
      <% end %>
    </div>
  </div>

  <div class="d-flex gap-2 mt-4">
    <%= form.submit class: "btn btn-primary" %>
    <%= link_to "Cancel", (subject.persisted? ? subject : subjects_path), class: "btn btn-outline-secondary" %>
  </div>
<% end %>
