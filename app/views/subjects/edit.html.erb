<% content_for :title, "Edit #{@subject.name}" %>

<div class="page-header">
  <div>
    <p class="page-eyebrow">Subjects</p>
    <h1 class="h2">Edit Subject</h1>
  </div>
</div>

<%= render "form", subject: @subject %>
